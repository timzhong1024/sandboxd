{
  "name": "@sandboxd/cli",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "sandboxctl": "./dist/cli.js"
  },
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@sandboxd/control-plane": "workspace:*",
    "@sandboxd/core": "workspace:*"
  }
}
